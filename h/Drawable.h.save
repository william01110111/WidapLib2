#include "Vector.h"
#include "Color.h"

///this declares the Drawable superclass that you can draw to

#pragma once

namespace widap
{

class Drawable
{
public:
	void setDrawClr(ClrBGR in);
	void setDrawVals(ClrBGR inClr, double inAlpha);
	void setLoc
	
	void rect(V2d low, V2d hgh, ClrBGR inClr, double inAlpha);
	void rect(V2d low, V2d hgh, ClrBGR inClr);
	virtual void rect(V2d low, V2d hgh)=0;
	
private:
	///state variables
	//these will be frequently changed and are used for drawing
	ClrBGR drawClr;
	double drawAlpha;
	V2d loc0;
};

}
